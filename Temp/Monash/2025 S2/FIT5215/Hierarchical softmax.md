
原理：使用二叉树结构，将复杂度从 O(V) 降到 O(log V)

二叉树示例：
              根
           /      \
          /        \
        节点1      节点2
       /   \       /   \
     "我" "爱" "自然" "语言"

预测路径：从根节点到目标词的路径
预测 "自然"：根 → 右 → 左
概率计算：P("自然") = P(右|根) × P(左|节点2)