---
date: 2025-09-01
author:
  - Siyuan Liu
tags:
  - FIT5215
---
**理解：**
通过精心设计的微小扰动来欺骗机器学习模型，以及如何防御这些攻击的领域。这些扰动对人类来说几乎不可见，但可以导致模型做出完全错误的预测
```
# 目标：最大化模型的损失
# 方法：沿着梯度方向修改输入

# 正常训练：调整参数θ来减小损失
θ_new = θ - α × ∇_θ L

# 对抗攻击：调整输入x来增大损失
x_adv = x + ε × sign(∇_x L)
```



# 攻击类型
## 白盒攻击（White-box Attack）
```
# 攻击者完全了解模型：
# - 模型架构
# - 模型参数
# - 训练数据

# 可以直接计算梯度
gradients = model.backward(x, y)
perturbation = epsilon * sign(gradients)
x_adv = x + perturbation
```

---

## 黑盒攻击（Black-box Attack）
```
# 攻击者只能查询模型：
# - 不知道模型结构
# - 不知道参数
# - 只能看到输出

# 方法1：训练替代模型
substitute_model = train_similar_model()
# 在替代模型上生成对抗样本
x_adv = generate_adversarial(substitute_model, x)
# 希望能迁移到目标模型

# 方法2：基于查询的攻击
# 通过大量查询估计梯度
```

---
## 目标攻击 
```
# 目标攻击（Targeted）：
# 让模型预测为特定的错误类别
# 例如：把"熊猫"预测为"长臂猿"
# 目标：min L(model(x_adv), y_target)
```

---
## 非目标攻击
![[Pasted image 20251001101301.png]]
```
# 非目标攻击（Untargeted）：
# 只要让模型预测错误即可
# 目标：max L(model(x_adv), y_true)
```


---
# 经典攻击模型
## FGSM (Fast Gradient Sign Method)
**理解：**
```
原始图像 x
    ↓ 前向传播
损失 L(x, y)
    ↓ 反向传播
梯度 ∇_x L
    ↓ 取符号
sign(∇_x L) ∈ {-1, 0, +1}
    ↓ 缩放
ε × sign(∇_x L)
    ↓ 添加到原图
x_adv = x + ε × sign(∇_x L)
```

**计算过程：**
$$
x_{adv} = x + \epsilon \cdot \text{sign}(\nabla_x L(f(x;\;\theta), y))
$$
$$sign(t) = \begin{cases}
1 &\text{if t}\gt 0\\  
-1 &\text{if t}\lt 0\\
0 &\text{otherwise}\\
\end{cases}$$
- $x:$ 原始输入
- $x_{adv}:$ 对抗样本
- $ε:$ 扰动强度（通常很小，如0.01）
- $L:$ 损失函数
- $∇_x L:$ 损失函数关于输入x的梯度
- $sign():$ 符号函数（只保留方向）
- $θ:$ 模型参数
- $y:$ 真实标签

---
## PGD (Projected Gradient Descent)
**理解：**
![[Pasted image 20251001104131.png]]

**计算过程：**
$$\begin{aligned}
& x_0 = x+ Uniform([-\epsilon,\epsilon])\\
& \tilde{x}_{t+1} = x_t + \eta \nabla_x L(f(x;\;\theta))\\
& x_{t+1} = Proj_{B_{\epsilon}(x)}(\tilde{x}_{t+1})\\
& 𝑥_{𝑎𝑑𝑣}= 𝑥_k
\end{aligned}$$
- $k$: Run in 𝑘 steps (𝑘 = 20)
- $𝜂 > 0$: is the learning rate